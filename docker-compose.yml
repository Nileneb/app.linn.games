services:
  web:
    image: nileneb/linn-web:latest
    restart: unless-stopped
    volumes:
      - linn-storage-production:/var/www/storage:ro
      - ./public:/var/www/public:ro
      - linn-build-assets:/var/www/public/build:ro
      - ./docker/common/nginx/default.conf:/etc/nginx/templates/default.conf.template:ro
      - ./docker/mayring/nginx/mcp-passthrough.conf:/etc/nginx/conf.d/mcp.conf:ro
    environment:
      - MCP_AUTH_TOKEN=${MCP_AUTH_TOKEN}
      - MCP_SERVICE_TOKEN=${MCP_SERVICE_TOKEN}
    networks:
      - linn-network
      - linn-shared
    ports:
      - "${APP_PORT:-6481}:80"
    depends_on:
      - php-fpm

  php-fpm:
    image: nileneb/linn-php:latest
    restart: unless-stopped
    volumes:
      - linn-storage-production:/var/www/storage
      - linn-build-assets:/var/www/public/build-shared
      - ./public/images:/var/www/public/images:ro
    env_file:
      - .env
    networks:
      - linn-network
      - linn-shared
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  php-cli:
    image: nileneb/linn-php:latest
    tty: true
    stdin_open: true
    env_file:
      - .env
    networks:
      - linn-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  php-test:
    profiles: [test]
    build:
      context: .
      dockerfile: ./docker/common/php-fpm/dockerfile
      target: testing
    entrypoint: []
    tty: true
    stdin_open: true
    env_file:
      - .env
    environment:
      - APP_ENV=testing
      - DB_CONNECTION=pgsql
      - DB_HOST=postgres
      - DB_PORT=5432
      - MAIL_MAILER=array
      - BROADCAST_CONNECTION=null
      - QUEUE_CONNECTION=sync
      - SESSION_DRIVER=array
      - CLAUDE_API_KEY=
    volumes:
      - ./tests:/var/www/tests:ro
      - ./phpunit.xml:/var/www/phpunit.xml:ro
      - ./.env:/var/www/.env:ro
    networks:
      - linn-network

  queue-worker:
    image: nileneb/linn-php:latest
    restart: unless-stopped
    command:
      [
        "php",
        "artisan",
        "queue:work",
        "--sleep=3",
        "--tries=3",
        "--max-time=3600",
      ]
    volumes:
      - linn-storage-production:/var/www/storage
    env_file:
      - .env
    networks:
      - linn-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  reverb:
    image: nileneb/linn-php:latest
    command: php artisan reverb:start --host=0.0.0.0 --port=8080
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - linn-storage-production:/var/www/storage
    networks:
      - linn-network
    depends_on:
      redis:
        condition: service_healthy

  postgres:
    image: nileneb/linn-postgres:latest
    restart: unless-stopped
    user: postgres
    command: ["postgres", "-c", "shared_preload_libraries=pg_stat_statements"]
    environment:
      - POSTGRES_DB=${POSTGRES_DATABASE}
      - POSTGRES_USER=${POSTGRES_USERNAME}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - linn-postgres-data:/var/lib/postgresql/data
      - ./docker/common/postgres/init-extensions.sql:/docker-entrypoint-initdb.d/init-extensions.sql:ro
    networks:
      - linn-network
      - linn-shared
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\""]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    restart: unless-stopped
    networks:
      - linn-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  mcp-paper-search:
    image: nileneb/linn-paper-search:latest
    restart: unless-stopped
    ports:
      - "8089:8089"
    environment:
      - BEARER_TOKEN=${MCP_SERVICE_TOKEN:-${MCP_AUTH_TOKEN}}
      - MCP_TRANSPORT=streamable-http
      - MCP_MESSAGES_PATH=/paper-messages/
      - DOWNLOAD_PATH=/data/papers
      - LARAVEL_INGEST_URL=http://php-fpm/api/papers/ingest
      - LARAVEL_MCP_TOKEN=${MCP_SERVICE_TOKEN:-${MCP_AUTH_TOKEN}}
      - LARAVEL_DB_HOST=postgres
      - LARAVEL_DB_PORT=5432
      - LARAVEL_DB_USER=${POSTGRES_USERNAME}
      - LARAVEL_DB_PASSWORD=${POSTGRES_PASSWORD}
      - LARAVEL_DB_NAME=${POSTGRES_DATABASE}
      - PAPER_SEARCH_MCP_UNPAYWALL_EMAIL=${PAPER_SEARCH_MCP_UNPAYWALL_EMAIL:-}
      - PAPER_SEARCH_MCP_CORE_API_KEY=${PAPER_SEARCH_MCP_CORE_API_KEY:-}
      - PAPER_SEARCH_MCP_SEMANTIC_SCHOLAR_API_KEY=${PAPER_SEARCH_MCP_SEMANTIC_SCHOLAR_API_KEY:-}
      - PAPER_SEARCH_MCP_ZENODO_ACCESS_TOKEN=${PAPER_SEARCH_MCP_ZENODO_ACCESS_TOKEN:-}
      - PAPER_SEARCH_MCP_DOAJ_API_KEY=${PAPER_SEARCH_MCP_DOAJ_API_KEY:-}
      - PAPER_SEARCH_MCP_GOOGLE_SCHOLAR_PROXY_URL=${PAPER_SEARCH_MCP_GOOGLE_SCHOLAR_PROXY_URL:-}
      - PAPER_SEARCH_MCP_IEEE_API_KEY=${PAPER_SEARCH_MCP_IEEE_API_KEY:-}
      - PAPER_SEARCH_MCP_ACM_API_KEY=${PAPER_SEARCH_MCP_ACM_API_KEY:-}
    volumes:
      - linn-papers-data:/data/papers
    networks:
      - linn-network
    depends_on:
      php-fpm:
        condition: service_started
      postgres:
        condition: service_healthy

networks:
  linn-network:
    driver: bridge
  linn-shared:
    external: true

volumes:
  linn-postgres-data:
  linn-storage-production:
  linn-build-assets:
  linn-papers-data:
    external: true
