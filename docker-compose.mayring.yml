# MayringCoder Stack — deployed independently from app.linn.games stack.
# Prerequisite: docker network create linn-shared (once on server)
# Deploy: ./deploy-mayring.sh

services:
  mayring-nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "${MAYRING_PORT:-6480}:80"
    volumes:
      - ./docker/mayring/nginx/mcp.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - mayring-internal
      - linn-shared
    depends_on:
      - mayring-webui
      - mayring-mcp

  mayring-api:
    image: nileneb/mayring:latest
    command: python -m uvicorn src.api.server:app --host 0.0.0.0 --port 8090
    restart: unless-stopped
    env_file: .env.production
    environment:
      LARAVEL_INTERNAL_URL: http://web
    volumes:
      - linn-mayring-cache:/app/cache
      - ./reports:/app/reports
      - ./secrets:/secrets:ro
      - linn-papers-data:/data/papers:ro
    networks:
      - mayring-internal
      - linn-shared
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 60s

  mayring-mcp:
    image: nileneb/mayring:latest
    command: python -m src.api.mcp
    restart: unless-stopped
    env_file: .env.production
    environment:
      MCP_TRANSPORT: http
      MCP_HTTP_PORT: 8092
      MCP_HTTP_HOST: 0.0.0.0
      MCP_AUTH_ENABLED: "true"
    volumes:
      - linn-mayring-cache:/app/cache
      - ./secrets:/secrets:ro
    networks:
      - mayring-internal
      - linn-shared
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8092/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  mayring-pi:
    image: nileneb/mayring:latest
    command: python -m src.agents.pi_server
    restart: unless-stopped
    env_file: .env.production
    volumes:
      - linn-mayring-cache:/app/cache
      - ./secrets:/secrets:ro
    networks:
      - mayring-internal
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8091/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  mayring-webui:
    image: nileneb/mayring:latest
    command: python -m src.api.web_ui --host 0.0.0.0
    restart: unless-stopped
    env_file: .env.production
    environment:
      LARAVEL_INTERNAL_URL: http://web
      GRADIO_ROOT_PATH: /ui
    volumes:
      - linn-mayring-cache:/app/cache:ro
      - ./secrets:/secrets:ro
    networks:
      - mayring-internal
      - linn-shared
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7860"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Issue #52 — inkrementeller Claude-Conversation-Watcher.
  # Activate on a dev host (where ~/.claude/projects exists):
  #   CLAUDE_PROJECTS_DIR=~/.claude/projects \
  #     docker compose -f docker-compose.mayring.yml --profile watcher up -d mayring-watcher
  # Post-hook triggers /wiki/generate on the api so Wiki/Ambient/Predictive
  # stay fresh without restarting mayring-api.
  mayring-watcher:
    image: nileneb/mayring:latest
    profiles: ["watcher"]
    restart: unless-stopped
    env_file: .env.production
    environment:
      CLAUDE_PROJECTS_DIR: "/host_claude"
      MAYRING_API_URL: "http://mayring-nginx:80"
      WATCHER_WORKSPACE_ID: "${WATCHER_WORKSPACE_ID:-system}"
    command:
      - python
      - -m
      - tools.conversation_watcher
      - --workspace-id
      - "system"
      - --hook-cmd
      - "curl -fsS -X POST -H 'Authorization: Bearer ${MCP_SERVICE_TOKEN}' -H 'Content-Type: application/json' -d '{\"workspace_id\":\"${WATCHER_WORKSPACE_ID:-system}\"}' http://mayring-api:8090/wiki/generate >/dev/null"
      - --hook-interval
      - "1800"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "conversation_watcher"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 10s
    volumes:
      - linn-mayring-cache:/app/cache
      - linn-watcher-state:/root/.cache/mayryngcoder
      - ${CLAUDE_PROJECTS_DIR:-/tmp/empty_claude_projects}:/host_claude:ro
    networks:
      - mayring-internal
    depends_on:
      mayring-api:
        condition: service_healthy

networks:
  mayring-internal:
    driver: bridge
  linn-shared:
    external: true

volumes:
  linn-mayring-cache:
    external: true
  linn-papers-data:
    external: true
  linn-watcher-state:
    name: linn-watcher-state
